<h1 id='untitled-1'>Untitled-1</h1><h2 id='connects-to-the-database-and-attempts-to-apply-all-adds-updates-and-deletes'>Connects to the database and attempts to apply all adds,  updates and deletes</h2>
<table><thead>
<tr>
<th>Parameter</th>
<th>Description</th>
</tr>
</thead><tbody>
<tr>
<td>&quot;data&quot;</td>
<td>a dataset, passed by reference,  that contains all the  data for updating</td>
</tr>
<tr>
<td>&quot;data&quot;</td>
<td>a dataset, passed by reference,  that contains all the  data for updating</td>
</tr>
</tbody></table>

<blockquote>
<p>This sample shows how to call the SaveData  method from a wireless device.</p>
</blockquote>
<pre class="highlight csharp tab-csharp"><code>
<span class="n">AccidentCRUD</span> <span class="n">accCRUD</span> <span class="p">=</span> <span class="k">new</span> <span class="n">Adjuster</span><span class="p">.</span><span class="n">BusinessServices</span><span class="p">.</span><span class="nf">AccidentCRUD</span><span class="p">();</span>
<span class="n">accCRUD</span><span class="p">.</span><span class="nf">SaveData</span><span class="p">(</span><span class="k">ref</span> <span class="n">ds</span><span class="p">);</span>
</code></pre>